name: Intake_monitor_time_bins
channels:
  - defaults
dependencies:
  - python=3.7
  - pip
  - pip:
    - PySimpleGUI
    - tqdm
    - datetime
    - numpy
    - pandas
    - openpyxl
    - Jinja2
